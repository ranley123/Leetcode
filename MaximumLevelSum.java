/**
 * @id 1161
 * @author ranley
 * @level middle
 * @runtime 8ms faster than 92.38%
 */
public class MaximumLevelSum {
	
////	List<Integer> res = new ArrayList<>();
////  public int maxLevelSum(TreeNode root) {
////      helper(root, 1);
////      int max = Integer.MIN_VALUE;
////      int level = -1;
////      for(int i = 0; i < res.size(); i++){
////          int temp = res.get(i);
////          if(temp > max){
////              max = temp;
////              level = i + 1;
////          }
////      }
////      return level;
////  }
// 
////  private void helper(TreeNode cur, int depth){
////      if(cur == null)
////          return;
////      if(res.size() >= depth){
////          res.set(depth - 1, res.get(depth - 1) + cur.val);
////      }
////      else{
////          res.add(cur.val);
////      }
////      helper(cur.left, depth + 1);
////      helper(cur.right, depth + 1);
////  }
	
	// method 2
	
}
